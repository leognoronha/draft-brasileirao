<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/draftbrasil"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/draftbrasil_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DraftBrasileirao"
        tools:targetApi="31">
        <activity
            android:name=".FinalizationActivity"
            android:exported="false" />
        <activity
            android:name=".TeamSummaryActivity"
            android:exported="false" />
        <activity
            android:name=".PlayerSelectionActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            android:exported="false" />
        <activity
            android:name=".FormationActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.DraftBrasileirao">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>